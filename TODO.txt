TODO:
	implement testing - assert
	scroll through recent terminal commands using arrows
	refactor the if blocks into separate functions so its not cancer on your eyes
	
FIXME:
	save on modified only works once you load the file
	create config file doesn't work atm, must have changed when I changed the way I read the file